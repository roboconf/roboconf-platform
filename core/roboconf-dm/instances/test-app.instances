instance of vm {
	name: mysql-vm;
	instance-state: NOT_DEPLOYED;

	instance of mysql {
		name: mysql-server;
		instance-state: NOT_DEPLOYED;
	}
}


instance of vm {
	name: tomcat-vm;
	instance-state: NOT_DEPLOYED;

	instance of tomcat {
		name: tomcat-server;
		instance-state: NOT_DEPLOYED;

		instance of war {
			name: hello-world;
			instance-state: NOT_DEPLOYED;
		}
	}
}

