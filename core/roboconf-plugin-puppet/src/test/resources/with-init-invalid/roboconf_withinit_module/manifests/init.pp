class roboconf_withinit_module($runningState = undef, $importAdded = undef, $importRemoved = undef, $withoperations = undef) {

  # What happens with an invalid Puppet script?
  And here comes an invalid command! :P
}
