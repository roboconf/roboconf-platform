# List of the Puppet modules to install
# so that our manifests can run.

# Key = module name
# Value = module version (let it empty to use the last one)

# Modules will be installed under
# <temp-directory>/roboconf_agent/puppet/<instance-name>/modules
roboconf-roboconf = wrong.version
